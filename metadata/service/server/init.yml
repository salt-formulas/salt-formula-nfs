applications:
- nfs
parameters:
  nfs:
    server:
      enabled: true
      share:
        srv_dir:
          path: /srv
          host: 
            inter:
              host: 192.168.50.0/24
              params:
              - rw
              - no_root_squash
              - sync